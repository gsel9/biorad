# -*- coding: utf-8 -*-
#
# Configuration setup file for PyRadiomics shape feature extraction.
#
# PyRadiomics version: 2.0.1
#

setting:
  # Value of ROI in mask is binary where zero represents the background.
  label: 1
  # Disable output of additional information from the extraction.
  additionalInfo: False


featureClass:
  # The shape descriptors are extracted from the binary image tumor mask only.
  shape:
